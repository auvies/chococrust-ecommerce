# Documentation Index

- [Root README](../README.md) — what Choco Crust is, business context, high-level architecture.
- [CLAUDE.md](../CLAUDE.md) — permanent engineering/security/process rules.
- [frontend/README.md](../frontend/README.md) — frontend setup, scripts, structure.
- [backend/README.md](../backend/README.md) — backend setup, scripts, structure.
- [adr/](adr/) — Architecture Decision Records, one file per significant decision, numbered sequentially. Add a new ADR rather than editing history when a past decision is revisited.
  - [0001-initial-architecture.md](adr/0001-initial-architecture.md) — Next.js/Laravel/PostgreSQL stack, provider-replaceable database, frontend/backend separation.
  - [0002-database-schema.md](adr/0002-database-schema.md) — the Phase 02 normalized schema: status separation, hierarchical categories, inventory reservations, soft-delete choices, and the portability tradeoffs behind them.
  - [0003-authentication-and-authorization.md](adr/0003-authentication-and-authorization.md) — the Phase 03 auth/security foundation: hand-rolled JWT, the three-cookie session model, refresh rotation, RBAC roles/permissions, and how AI agent identities are structurally kept out of the admin surface.
  - [0004-api-foundation.md](adr/0004-api-foundation.md) — the Phase 04 REST API over all 19 business modules: the shared query/response/idempotency/audit-logging infrastructure, where business logic lives, and two real mass-assignment/morph-map bugs the module tests caught.
  - [0005-public-storefront.md](adr/0005-public-storefront.md) — the Phase 05 Next.js storefront: force-dynamic rendering, deriving "Best Sellers"/"Offers" from real signals instead of hardcoding, the rate-limit issue found by testing against a real backend, and id+slug routing.
  - [0006-admin-panel.md](adr/0006-admin-panel.md) — the Phase 06 admin panel: client-rendered direct-to-API architecture, RBAC enforced at three layers, session refresh-retry, the four small backend gaps found and closed, and a stale-DOM test-isolation bug fixed globally.
  - [0007-dynamic-catalog-and-homepage.md](adr/0007-dynamic-catalog-and-homepage.md) — the Phase 07 dynamic catalog/homepage system: category-hierarchy filtering, sales-based best sellers with a reused admin-override mechanism, homepage composition driven by `homepage_sections`, and generalizing `/search` into a browse+filter page.
  - [0008-cart-checkout-and-delivery-eligibility.md](adr/0008-cart-checkout-and-delivery-eligibility.md) — the Phase 08 cart/checkout/order system: deny-by-default delivery eligibility scoped through the category tree, the Khanewal local-delivery zone as `delivery_rules` data, a client-only cart, address-ownership and snapshot fixes, a delivery-eligibility preview endpoint, and customer order tracking reusing the existing delivery policy.
  - [0009-payment-cod-inventory-delivery.md](adr/0009-payment-cod-inventory-delivery.md) — the Phase 09 payment/COD/inventory/delivery hardening: converting status enums to app-validated strings, mapping the COD flow across three independent state machines, a real inventory sell-through lifecycle with append-only history, config-driven payment methods with no secrets ever reaching the frontend, idempotent COD actions, and testing concurrency as a check-then-act guard.
  - [0010-customer-crm-and-notifications.md](adr/0010-customer-crm-and-notifications.md) — the Phase 10 customer CRM and notifications work: a structured append-only note log and an admin-curated tag system with deliberately different RBAC defaults, a real server-side rating aggregate replacing a partial-page client computation, a trigger → template → channel notification pipeline for order/payment/delivery events, and WhatsApp prepared-not-built the same way Easypaisa/JazzCash was in Phase 09.
  - [0011-media-themes-hero-banners-seo-social.md](adr/0011-media-themes-hero-banners-seo-social.md) — the Phase 11 media/themes/hero-banners/SEO/social work: one shared secure-upload service reused by product/category/hero-banner images, a validated theme-config shape seeded twelve times over, a fuller hero-banner lifecycle (upload/reorder/archive-restore/preview), Open Graph as its own SEO field, Facebook/Instagram prepared-not-built the same way WhatsApp was in Phase 10, and a real pre-existing CSRF gap on category/product mutations found and fixed.
  - [0012-ai-chatbot.md](adr/0012-ai-chatbot.md) — the Phase 12 AI chatbot: a real intent-detection → deterministic-lookup → relevant-data-retrieval → AI-only-when-necessary pipeline where prices/stock/delivery/hours/payment methods/order status/contact never cost a model call, two independent rate/cost controls, deterministic history summarization, three layers of prompt-injection defense, and a real (not stubbed) Anthropic provider that gracefully degrades with no API key configured.
  - [0013-ai-agent-architecture.md](adr/0013-ai-agent-architecture.md) — the Phase 13 future AI-agent architecture: one generalized tool gateway (Agent → Approved Tool → Validation → Authorization → Business Logic → Database) replacing the narrower Phase 12 tool surface, agent-type-scoped authorization via a new `users.agent_type` column, high-risk operations routed to a human-approval queue that requires the same permission the action itself would, and a 9-event architecture with one logging listener - deliberately none of the 8 named agents actually built yet.
  - [0014-analytics-audit-and-ai-cost-governance.md](adr/0014-analytics-audit-and-ai-cost-governance.md) — the Phase 14 analytics/audit/AI-cost work: eight new analytics endpoints extracted into a real `AnalyticsService`, a code-level redaction backstop on every audit-log write, durable security-event logging, a cross-cutting `AiUsageLimiter` covering both the chatbot and the Phase 13 agent tool-call surface, and the backend test suite actually executed for the first time - finding and fixing one real pre-existing bug and surfacing 18 more, out of scope, for follow-up.
